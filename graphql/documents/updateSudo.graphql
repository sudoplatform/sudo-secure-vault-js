mutation UpdateVault($input: UpdateVaultInput!) {
  updateVault(input: $input) {
    id
    version
    createdAtEpochMs
    updatedAtEpochMs
    owner
    blobFormat
    encryptionMethod
    owners {
      id
      issuer
    }
  }
}
