mutation DeleteVault($input: DeleteVaultInput!) {
  deleteVault(input: $input) {
    id
    version
    createdAtEpochMs
    updatedAtEpochMs
    owner
    blobFormat
    encryptionMethod
    owners {
      id
      issuer
    }
  }
}
